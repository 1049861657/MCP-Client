---
description: MCP-Client TypeScript 编码约束
alwaysApply: true
---

# MCP-Client

## 目录
- `src/api/` 薄控制器/路由 | `services/` 业务 | `core/` 核心 | `servers/` 外部适配 | `config/` 常量 | `types/` `interfaces/` 类型 | `lib/` 封装 | `utils/` 纯函数 | `prisma/` schema
- 新代码放最匹配目录；禁止跨层堆逻辑

## 命名
- 类 PascalCase | 方法/变量 camelCase | 常量 UPPER_SNAKE_CASE | 文件 kebab-case
- interface/type PascalCase，**禁止 I 前缀**

## TS
- 公共函数显式返回类型；禁 `any`，用 `unknown` 收窄
- 可选参数只用 `?`；结构 `interface`，联合/交叉 `type`
- 命名导出；禁 default export
- import 顺序：node → 第三方 → 项目内，组内字母序；相对路径带 `.js`；禁循环依赖

## 错误
- 底层：失败 `throw`；禁空 catch、禁静默 fallback 掩盖配置缺失
- 边界层（api）：catch 后返回明确错误，不吞异常
- try/catch 仅用于补上下文或资源清理

## 异步
- async/await；禁 .then 链；无依赖 IO 用 Promise.all；禁顶层 await；长操作传 AbortSignal

## Windows 终端（Agent）
- Agent Shell 为 **PowerShell**（非 cmd）；禁止 CMD 专用语法

## 包管理
- 使用 **pnpm**；禁 `npm` 

## 其他
- 魔法数/超时/上限 → config 或 ConfigService
- 最小 diff；扩展现有模块，禁重复造轮子；文件过长按职责拆分
